[pytest]
testpaths = tests
pythonpath = src
markers =
    hardware: requires physical Meshtastic radios connected via USB
    pi: integration tests requiring two Meshtastic radios, combo webui, sequential run
addopts = --strict-markers --strict-config -q
